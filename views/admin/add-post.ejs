<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PreMedTalk</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/add-post.css">
</head>
<body>
    <div class="content-wrapper">
        <a href="/dashboard" aria-label="Back to dashboard">&larr; Back</a>

        <div class="admin-title">
            <h2>Add New Post</h2>
        </div>

        <form action="/add-post" method="POST" enctype="multipart/form-data">
            <label for="title"><b>Title</b></label>
            <input type="text" placeholder="Enter Post Title" id="title" name="title" required>

            <label for="postBodyTextarea"><b>Content</b></label>
            <div id="postBody" contenteditable="true" style="border: 1px solid #ccc; padding: 10px; min-height: 200px;"></div>
            <textarea id="postBodyTextarea" name="body" hidden required></textarea>

            <label for="postImage"><b>Upload Image</b></label>
            <input type="file" id="postImage" name="postImage" accept="image/*">

            <input type="submit" value="Add" class="btn">
        </form>
    </div>

    <script>
        document.querySelector('form').addEventListener('submit', function (event) {
            const postBodyDiv = document.getElementById('postBody');
            const postBodyTextarea = document.getElementById('postBodyTextarea');

            // Assign the innerHTML of the contenteditable div to the hidden textarea
            postBodyTextarea.value = postBodyDiv.innerHTML;

            // Debugging: Log the HTML content being submitted
            console.log("HTML content submitted:", postBodyTextarea.value);
        });
    </script>
</body>
</html>